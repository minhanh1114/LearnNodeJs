<div class='cmt-4'>
    <div class='row'>
        <div class='col-lg-3'>
            <button>Hoc ngay</button>
        </div>

        <div class='col-lg-9'>
            <h2>{{course.name}}</h2>
            <iframe
                width='500'
                height='315'
                src='https://www.youtube.com/embed/{{course.videoId}}'
                frameborder='0'
            ></iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
    {{course.name}}
</div>